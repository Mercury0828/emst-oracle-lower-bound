# Local simulation environment (do not commit; recreate per sim/README.md)
sim/.venv/

# Python caches
__pycache__/
*.pyc

# Figures and results are regenerated by sim/run_all.py; keep them tracked so the
# frozen Phase-0 numbers are reviewable without rerunning. (Comment out to ignore.)
# sim/figures/
# sim/results.json
